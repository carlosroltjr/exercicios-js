console.log(soma(3, 4)); // antes da funcao ser declarada

// function declaration
function soma (x, y) {
    return x + y;
}

// function expression
const subtracao = function (x, y) {
    return x - y;
}

// named function expression
const multiplicacao = function multiplicacao (x, y) {
    return x * y;
}

